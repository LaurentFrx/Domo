# Node / pnpm
node_modules/
.pnpm-store/

# SvelteKit
.svelte-kit/
build/
.vercel/

# Vite
.vite/
dist/

# Logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Environnement
.env
.env.local
.env.*.local

# IDE
.idea/
*.swp
*.swo
.claude/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Tests
coverage/
playwright-report/
test-results/

# Docker
.dockerignore
data/

# thermostat-bridge — artefacts runtime (le daemon tourne sur le RPi4)
thermostat-bridge/state.json
thermostat-bridge/__pycache__/
thermostat-bridge/.venv/

# Backups .env (contiennent des secrets)
.env.bak*

# Rapports d'audit de SÉCURITÉ — ce dépôt est PUBLIC. Ces rapports citent le
# domaine réel, la topologie des tunnels et le détail de failles pas encore
# corrigées : les garder hors git (les copies vivent dans ~/files, servi par
# Filebrowser derrière l'auth). Les audits de FIABILITÉ, eux, restent versionnés.
docs/audit-ultracode-*.md
docs/audit-securite-*.md
